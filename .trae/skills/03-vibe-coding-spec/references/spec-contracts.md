# 核心契约模板

根据组件类型选择对应的核心契约模板。核心契约是最先设计的部分，错误时修正代价最大。

---

## Web/App 产品：数据模型

```markdown
### 数据模型

#### 实体定义
| 实体 | 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| [实体名] | [字段名] | [类型] | [必填/唯一/索引] | [说明] |

#### 实体关系
| 关系 | 类型 | 说明 |
|------|------|------|
| [实体A] → [实体B] | 一对多 / 多对多 / 一对一 | [说明] |

#### 状态机（如有）
[实体名] 状态流转：
[初始状态] → [状态B] → [状态C]
                ↓
            [状态D]

#### 索引策略
| 索引 | 字段 | 类型 | 说明 |
|------|------|------|------|
| [索引名] | [字段] | 唯一/普通/复合 | [查询场景] |
```

---

## AI 功能：Prompt 规格 + 输出 Schema

```markdown
### Prompt 规格

#### System Prompt
[完整的 System Prompt 文本，标注变量占位符 {{variable}}]

#### User Prompt 模板
[完整的 User Prompt 模板，标注变量占位符 {{variable}}]

#### 上下文构建策略
| 上下文来源 | 获取方式 | Token 预算 | 截断策略 |
|-----------|---------|-----------|---------|
| [来源1] | [方式] | [数量] | [策略] |

#### 模型参数
| 参数 | 值 | 说明 |
|------|-----|------|
| model | [名称] | |
| temperature | [值] | |
| max_tokens | [值] | |
| top_p | [值] | |

### 输出 Schema

[JSON Schema 定义]

```json
{
  "type": "object",
  "properties": {
    "[字段名]": {
      "type": "[类型]",
      "description": "[说明]"
    }
  },
  "required": ["[必填字段]"]
}
```

### 输出校验规则

| 规则 | 类型 | 校验方式 | 失败处理 |
|------|------|---------|---------|
| [规则1] | 格式/内容/安全 | [方式] | [重试/降级/拒绝] |

### 降级链路

校验失败 → 重试(1次, 同模型) → 切换模型 → 默认回复 → 提示用户

| 阶段 | 触发条件 | 行为 |
|------|---------|------|
| 重试 | 格式校验失败 | 同模型重试1次 |
| 切换模型 | 重试仍失败 | 切换到 [备用模型] |
| 默认回复 | 切换模型仍失败 | 返回 [默认内容] |
| 提示用户 | 默认回复不适用 | 显示 [提示信息] |
```

---

## 框架/库：公开 API 契约

```markdown
### 公开 API 契约

#### API 列表

| API | 签名 | 参数说明 | 返回值 | 异常 | 版本 |
|-----|------|---------|--------|------|------|
| [方法名] | [完整签名] | [参数详情] | [返回值详情] | [异常列表] | v1.0 |

#### 类型导出

| 类型 | 类型种类 | 说明 |
|------|---------|------|
| [类型名] | interface / type / class / enum | [用途说明] |

#### 扩展点

| 扩展点 | 触发时机 | 回调签名 | 执行顺序 | 默认行为 |
|--------|---------|---------|---------|---------|
| [钩子名] | [何时触发] | [签名] | [before/after] | [无/内置行为] |

#### 配置项

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| [配置名] | [类型] | [值] | [说明] |

#### Breaking Change 策略

- 遵循 semver
- 公开 API 签名变更 = major 版本
- 新增可选参数 = minor 版本
- 内部实现变更 = patch 版本
```

---

## 算法/模型：数学规格 + 维度推导

```markdown
### 数学规格

#### 核心公式

[数学公式，使用 LaTeX 或伪代码]

#### 维度推导

| 层 | 输入维度 | 操作 | 输出维度 | 参数量 |
|-----|---------|------|---------|--------|
| [层名] | [维度] | [操作] | [维度] | [数量] |

#### 前向传播路径

输入 → [层1] → [层2] → ... → 输出

#### 反向传播路径

损失 → ∂L/∂[层N] → ∂L/∂[层N-1] → ... → ∂L/∂[层1]

#### 参数初始化

| 参数 | 初始化策略 | 值/范围 |
|------|-----------|---------|
| [权重名] | [Xavier/He/自定义] | [值] |

#### 梯度处理

| 策略 | 配置 |
|------|------|
| 梯度裁剪 | [阈值/方式] |
| 梯度累积 | [步数] |
| 混合精度 | [float16/bfloat16/否] |

#### 数值稳定性

| 风险点 | 保护策略 |
|--------|---------|
| [除零/溢出/下溢] | [epsilon/裁剪/归一化] |
```

---

## CLI/工具：命令接口 + 参数规格

```markdown
### 命令接口

#### 命令树

[主命令]
├── [子命令1]
│   ├── [子子命令1a]
│   └── [子子命令1b]
├── [子命令2]
└── [子命令3]

#### 参数规格

##### [命令名]

| 参数 | 短选项 | 长选项 | 类型 | 必填 | 默认值 | 说明 |
|------|--------|--------|------|------|--------|------|

#### 退出码

| 退出码 | 含义 | 触发条件 |
|--------|------|---------|
| 0 | 成功 | 命令正常执行完成 |
| 1 | 一般错误 | 运行时错误 |
| 2 | 参数错误 | 参数校验失败 |

#### 输出格式

##### 默认输出（人类可读）
[格式描述]

##### JSON 输出（--json）
[JSON Schema]

#### 管道行为

| 输入 | 处理 | 输出 |
|------|------|------|
| stdin | [读取方式] | stdout |
| 文件参数 | [读取方式] | stdout / 文件 |
```

---

## API/后端服务：端点契约 + 数据模型

```markdown
### 端点契约

#### 端点列表

| 方法 | 路径 | 说明 | 鉴权 | 限流 |
|------|------|------|------|------|
| [GET/POST/PUT/DELETE] | /api/[path] | [说明] | [是/否] | [QPS] |

#### 端点详情

##### [方法] [路径]

**请求**:
| 字段 | 位置 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| [字段名] | body/query/path/header | [类型] | [是/否] | [说明] |

**响应**:
| 状态码 | 说明 | 响应体 |
|--------|------|--------|
| 200 | 成功 | [Schema] |
| 400 | 参数错误 | [Schema] |
| 401 | 未认证 | [Schema] |
| 403 | 无权限 | [Schema] |
| 500 | 服务器错误 | [Schema] |

### 数据模型

#### 实体定义
| 实体 | 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| [实体名] | [字段名] | [类型] | [必填/唯一/索引] | [说明] |

#### 实体关系
| 关系 | 类型 | 说明 |
|------|------|------|
| [实体A] → [实体B] | 一对多 / 多对多 | [说明] |
```

---

## 数据处理：数据 Schema + 管道拓扑

```markdown
### 数据 Schema

#### 输入 Schema

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| [字段名] | [类型] | [必填/格式/范围] | [说明] |

#### 输出 Schema

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| [字段名] | [类型] | [必填/格式/范围] | [说明] |

#### 中间 Schema（如有）

[每个管道步骤间的数据格式]

### 管道拓扑

[步骤A] → [步骤B] → [步骤C]
              ↓
          [步骤D] → [步骤E]

#### 管道步骤详情

##### [步骤名]

| 属性 | 值 |
|------|-----|
| 输入 | [Schema 引用] |
| 输出 | [Schema 引用] |
| 转换逻辑 | [描述] |
| 错误处理 | [跳过/默认值/终止] |
| 幂等性 | [是/否，保证方式] |

#### 数据质量规则

| 规则 | 适用步骤 | 校验方式 | 失败处理 |
|------|---------|---------|---------|
| [规则1] | [步骤名] | [方式] | [处理] |

#### 断点续传

| 属性 | 值 |
|------|-----|
| 执行标识 | [唯一键] |
| 偏移量记录 | [记录方式] |
| 恢复逻辑 | [描述] |
| 部分重跑 | [支持/不支持] |
```
